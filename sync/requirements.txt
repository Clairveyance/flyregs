requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
pypdf>=4.0.0
PyMuPDF>=1.24.0
anthropic>=0.116.0
numpy>=1.26.0
scipy>=1.11.0
# Only actually used by refresh_drs_jwt.py (LOI sync's DRS session
# auto-refresh) -- shared here rather than a separate requirements file
# since the browser BINARY download (the expensive part, ~260MB) is scoped
# to weekly-loi-sync.yml's own `playwright install chromium` step, not
# triggered just by this package being importable in other workflows.
playwright>=1.45.0
